import React from 'react'

import { images } from '../../utils'

const Logo = () => {
  return (
    <div className='form-logo'>
      <img src={images.logo} alt="logo" />
    </div>
  )
}

export default Logo
